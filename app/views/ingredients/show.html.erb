<h1><%= @ingredient.name %></h1>

<p id="notice"><%= notice %></p>

<p>
  <b>Color:</b>
  <%= @ingredient.color %>
</p>

<p>
  <b>Creator:</b>
  <%= link_to @ingredient.user.username, @ingredient.user %>
</p>

<p>
  <b>Used in these burgers:</b>
  <ul>
	<% @ingredient.burgers.each do |burger| %>
		<li><%= burger.name %> by <%= link_to burger.user.username, user_path(burger.user) %></li>
	<% end %>	
  </ul>
  
</p>

<%= link_to 'Edit', edit_ingredient_path(@ingredient) %> |
<%= link_to 'Back', ingredients_path %>
